[project]
name = "idioma"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "genanki>=0.13.1",
    "pydantic>=2.12.5",
    "pyyaml>=6.0.3",
]
